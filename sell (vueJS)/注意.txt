1、绑定请求数据中的图片或者给子组件传值是要使用 "v-bind" 或者 “:” 来绑定数据

2、当子元素都为display: inline-block时，为消除空白边距，可将父元素设置为font-size:0,或者将子元素写在一行

3、首页详细浮层sticky-footer的实现

4、用户评论显示的星星抽象为一个复用组件，要注意参数score的算法

5、后期维护是应当将弹层的 "优惠信息" 与 "商家公告" 抽象成组件

6、父元素display:flex; 子元素定义的flex有三个参数，第一个表示等分， 第二个表示内容的缩放情况， 第三个表示所在的位置(宽度)。

7、所有带hook的class都是没有实际意义的，只是供js操作DOM

8、给一个对象初始化未知属性并赋值，使用 vue.set('更改的对象', '添加的属性名', 初始化值)

9、vue2.x 组件列表呈现v-for应该有明确的key。

10、购物车小球抛物线的实现：由 payhandle 组件点击添加后向外派发一个事件
并传出当前点击的元素，后由父组件 goods 接收这个事件，尔后由父组件去调用 cart 组
件的接收事件并将点击的元素传入，最后实现小球的抛物线效果
一、由子组件 payhanlde 派送一个方法 'slideBall',并传出当前点击的元素
二、父组件 goods 接收该方法，并在 cart 组件定义 ref 
三、父组件使用$refs调用子组件 cart 的 swiperBall 方法实现组件之间的通信

